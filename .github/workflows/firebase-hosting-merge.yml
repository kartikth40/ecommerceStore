# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo ${{ secrets.REACT_APP_API }} | sed -e 's/\(.\)/\1 /g'
      - run: npm ci && REACT_APP_REGISTER_REDIRECT_URL=${{ secrets.REACT_APP_REGISTER_REDIRECT_URL }} REACT_APP_FORGOT_PASSWORD_REDIRECT_URL=${{ secrets.REACT_APP_FORGOT_PASSWORD_REDIRECT_URL }} REACT_APP_API=${{ secrets.REACT_APP_API}} REACT_APP_STRIPE_KEY=${{ secrets.REACT_APP_STRIPE_KEY }} CI=false npm run build
        working-directory: ./client
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_ANI_CART }}'
          channelId: live
          projectId: ani-cart
          entryPoint: ./client
